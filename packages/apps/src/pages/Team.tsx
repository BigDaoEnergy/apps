import { Box } from '@chakra-ui/react';
import React from 'react';

function Team() {
  return (
    <Box>
      Well it's just me right now, but the council is still open. Join early and
      become a core member!
    </Box>
  );
}

export default React.memo(Team);
